# 🌐 DB Homework  
### Нормализация данных до 3NF — Домашнее задание №1

<p align="left">
  <img src="https://img.shields.io/badge/PostgreSQL-4169E1?style=for-the-badge&logo=postgresql&logoColor=white"/>
  <img src="https://img.shields.io/badge/DBeaver-372923?style=for-the-badge&logo=dbeaver&logoColor=white"/>
  <img src="https://img.shields.io/badge/Excel-217346?style=for-the-badge&logo=microsoft-excel&logoColor=white"/>
</p>

---

## 📌 Описание проекта

Изначально был предоставлен один файл с данными о клиентах и транзакциях.  
На первом шаге данные были разделены на три отдельные таблицы:

- `DB_customer.csv`  
- `DB_product.csv`  
- `DB_transaction.csv`

Дальше стояла основная задача — привести данные к нормальной структуре, разделить по смысловым сущностям, убрать дублирование и логически связать таблицы. Итоговая модель построена по принципам **третьей нормальной формы (3NF)**.

---

## 🏗️ Что было сделано

### 1. Загружены исходные CSV-файлы
Все три файла были импортированы во временные таблицы (`stg_customers`, `stg_products`, `stg_transactions`).

### 2. Разделены данные клиентов
Из данных о клиентах выделены:

- таблица с данными о клиенте (`customers`);
- таблица с адресами (`customer_address`).

Адрес вынесен отдельно, потому что он не является характеристикой клиента, и в будущем у клиента может быть несколько адресов.

### 3. Созданы справочники (lookup-таблицы)
Чтобы убрать повторяющиеся текстовые значения, созданы таблицы:

- `genders`
- `wealth_segments`
- `job_industry_categories`
- `order_statuses`

Это снижает количество дублирующихся строк и делает данные аккуратнее.

### 4. Нормализованы данные по товарам
Таблица товаров разделена так, чтобы каждая характеристика имела своё место:

- `products` — сам товар  
- `product_price` — цена и себестоимость  
- справочники: `brands`, `product_lines`, `product_classes`, `product_sizes`

Это сделано для того, чтобы в описании товара не повторялись строки вроде "medium", "Standard", "Giant Bicycles" и т. д.

### 5. Приведены транзакции к чистому виду
В таблице транзакций оставлено только то, что относится к факту покупки:

- покупатель  
- товар  
- дата  
- онлайн/офлайн  
- статус заказа  

Статусы теперь находятся в отдельной таблице.

---

## 📊 Итоговая схема базы данных

### Первоначальная диаграмма (dbdiagram)

![Diagram](img/"dbdiagram на уровне проектирования.png")

### Финальная диаграмма (DBeaver)

![Diagram](img/dbeaver_diagram.png)

---

## 📁 Структура репозитория

```text
DB_homework/
│
├── data/
│   ├── DB_customer.csv
│   ├── DB_product.csv
│   └── DB_transaction.csv
│
├── img/
│   ├── dbeaver_diagram.png
│   ├── dbeaver_structure.jpg
│   └── dbdiagram на уровне проектирования.png
│
├── sql/
│   ├── 01_create_tables_and_lookups.sql
│   └── 02_load_data.sql
│
└── README.md
